import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  boxButton: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  button: {
    position: 'absolute',
    backgroundColor: '#28a889',
    borderRadius: 100,
    padding: 20,
  },
});
