import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    height: '30%',
    justifyContent: 'flex-end',
    marginBottom: 50,
  },
});
